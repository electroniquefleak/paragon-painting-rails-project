<div class="container">
    <div class="row">
        <div class="col-6 mx-auto">
            <h1>Add Team Members to Project:</h1>
            <h2><%= @project.name %></h2>
            <%= form_for [@project, @team] do |f| %>
                <%= render partial: "layouts/errors", locals: {record: @team} %>
                here is the form
                <%= f.submit %>
              <% end %>
        </div>
    </div>
</div>